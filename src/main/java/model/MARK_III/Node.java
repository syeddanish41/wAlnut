package model.MARK_III;

/**
 * Input during learning: long sequence of patterns that follow each other in time
 *
 * For every input pattern, the node does the following things:
 * 1) Memorize the pattern ?
 * 2) Learning transitional probabilities ?
 * 3) Temporal grouping?
 *
 * Created by qliu on 6/19/16.
 */
public class Node {
    // Memorization of input patterns
    // 1) Hashmap of distinct patterns this node sees.

    // Learning transition probabilities
    // 1) Markov graph where vertices correspond to the saved distinct patterns.
}
